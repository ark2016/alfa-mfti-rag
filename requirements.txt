# Гибкие пины — фиксируем major.minor, чтобы избежать «версия снята с PyPI»
# Установка см. setup_env.sh

# Core
numpy>=1.26,<2.0
pandas>=2.0,<3.0
pyarrow>=15.0
scikit-learn>=1.3
scipy>=1.11
tqdm>=4.66

# Embeddings + reranker
sentence-transformers>=3.0,<5.0
FlagEmbedding>=1.3.2,<2.0
faiss-cpu>=1.7
rank-bm25>=0.2.2

# LoRA + LLM (Qwen3.6 совместимость)
transformers>=4.55,<5.0
accelerate>=1.4
peft>=0.14
trl>=0.14
bitsandbytes>=0.45
datasets>=3.2
safetensors>=0.4.5

# Inference engine
vllm>=0.9.0,<0.11

# Misc
huggingface_hub>=0.26
einops>=0.8
sentencepiece>=0.2
protobuf>=4.25
